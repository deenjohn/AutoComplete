import React from 'react';
import ReactDOM from 'react-dom';
import Autosearch from './App';

ReactDOM.render(
  <React.StrictMode>
    <Autosearch />
  </React.StrictMode>,
  document.getElementById('root')
);
